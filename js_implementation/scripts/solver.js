import {ctx} from "./setup_grid.js"
import {gameOver} from "./sudoku.js"

export function solver(gridCopy) {
    if (!gameOver(gridCopy)) {
        // find where the next zero is (nextUnsolved)
        //
    }

}

function nextUnsolved(gridCopy) {
j
}

